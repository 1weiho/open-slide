{
  "name": "openslide-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --filter=playground",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "cli": "pnpm --filter openslide"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
