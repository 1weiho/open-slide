# open-slide Svelte workspace

Slides are Svelte components. Each deck lives under `slides/<id>/`, with an `index.ts` that imports and orders its page components.

```bash
pnpm install
pnpm dev
```

Use the arrow keys to move between pages. Press `F` for fullscreen, `I` for the inspector, or open the presenter view from the toolbar.
