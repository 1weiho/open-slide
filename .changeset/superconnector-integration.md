---
'@open-slide/core': minor
---

Add a "Run with agent" action to inspector comments. Each comment is dispatched through `@nimrobo/superconnector` to a connected coding agent, with run status streamed back to the UI over HMR.
