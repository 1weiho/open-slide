---
"@open-slide/core": patch
---

Share the remaining copy-pasted runtime helpers: one `pad2` for the 28 zero-padded counters, one `shortId` for comment and folder ids, one `GLOBAL_ASSET_SCOPE` constant, and one `dragHasFiles` for the three drop zones.
