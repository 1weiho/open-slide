---
"@open-slide/core": patch
"@open-slide/cli": patch
---

Warn in the inspector's replace/crop dialogs when an image is rendered from a shared component or map body, add a split-shared-images skill to refactor those slides, and update the slide-authoring skill to keep each `<img>` at its own call site.
