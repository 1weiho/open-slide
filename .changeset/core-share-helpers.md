---
"@open-slide/core": patch
---

Collapse duplicated runtime helpers into shared modules: one export-progress toast, one media-query hook, one set of keyboard predicates, one DOM helper module for the three exporters, and one slide-entry resolver and read-or-404 helper for the dev API routes.
