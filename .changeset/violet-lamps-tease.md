---
'@open-slide/core': patch
---

Fix StrictMode double-running undo/redo, label icon-only and inline-rename controls for screen readers, keep ref writes out of render, and speed up HTML export asset downloads.
