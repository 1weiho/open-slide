---
"@open-slide/core": minor
---

Enable the "Export as PPTX" download for editable, native PowerPoint output. The deck is rendered to real PowerPoint shapes (not images) by a built-in engine that uses Playwright. Playwright is an optional peer dependency: when it is missing, the export shows a copy-pasteable install command instead of a stream.
